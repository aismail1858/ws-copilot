

SUPABASE_API_URL= # Auto Configure in docker-compose file if running locally with containers
SUPABASE_SECRET_KEY=

JWT_SECRET=


AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=


REDIS_URL= # Auto Configure in docker-compose file if running locally with containers

REQUESTY_API_KEY=
REQUESTY_BASE_URL=https://api.requesty.ai/v1
REQUESTY_CHAT_MODEL=gpt-4o
REQUESTY_MINI_MODEL=gpt-4o-mini
REQUESTY_EMBEDDINGS_MODEL=text-embedding-3-large
REQUESTY_EMBEDDINGS_LLM_MODEL=gpt-4o
AGENT_MODEL_ID=openai:gpt-4o

# MCP-Server (agentic RAG nach aussen). Siehe ADR-003, KB-006.
# UUID eines existierenden Users in public.users, dessen Zugriffsmenge (accessible_document_ids)
# der MCP-Server nutzt. LEER = Server verweigert den Start (kein ungefilterter Fallback).
MCP_SERVICE_USER_ID=
MCP_HOST=0.0.0.0
MCP_PORT=8100

LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=

TAVILY_API_KEY=